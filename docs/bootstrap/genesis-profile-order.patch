diff --git a/src/genesis/creator.py b/src/genesis/creator.py
index a10e5e2..5e23540 100644
--- a/src/genesis/creator.py
+++ b/src/genesis/creator.py
@@ -709,6 +709,13 @@ def _transform_file_text(
         "the first Genesis-born agent",
         "a Genesis-born agent",
     )
+    if source.name == f"test_{source_agent.package}_profiles.py":
+        # Renaming the built-in profile can change its alphabetical position.
+        transformed = re.sub(
+            r'(self\.assertIn\("Available: )([a-z0-9_-]+(?:, [a-z0-9_-]+)*)(",)',
+            lambda match: match[1] + ", ".join(sorted(match[2].split(", "))) + match[3],
+            transformed,
+        )
     if source.name == f"test_{source_agent.package}_identity.py":
         transformed = re.sub(r"role: [a-z_]+_agent", "role: descendant_agent", transformed)
         transformed = transformed.replace(
diff --git a/tests/test_genesis_creator.py b/tests/test_genesis_creator.py
index 439a7e6..ee14e22 100644
--- a/tests/test_genesis_creator.py
+++ b/tests/test_genesis_creator.py
@@ -29,6 +29,36 @@ _PUBLIC_FIXTURE_TEMP: tempfile.TemporaryDirectory[str] | None = None
 
 
 class GenesisCreatorTests(unittest.TestCase):
+    def test_profile_order_survives_descent_in_both_alphabetical_directions(self) -> None:
+        with tempfile.TemporaryDirectory() as directory:
+            workspace = Path(directory)
+            source = workspace / "adam"
+            _clone_adam_source(source)
+            profile_test = source / "tests" / "test_adam_profiles.py"
+            profile_test.write_text(
+                "import unittest\n\n"
+                "class AdamProfilesTests(unittest.TestCase):\n"
+                "    def test_available_profiles(self):\n"
+                '        initial = "Available: " + ", ".join(sorted(["adam", "researcher"]))\n'
+                '        self.assertIn("Available: adam, researcher", initial)\n',
+                encoding="utf-8",
+            )
+            _commit_source_change(source, profile_test, "Add sorted profile fixture")
+            ancestor = "adam"
+            for name in ("ruth", "abel"):
+                with self.subTest(name=name):
+                    repo = workspace / name
+                    result = create_agent(
+                        name=name,
+                        ancestor=ancestor,
+                        mission="Preserve profile behavior across identity changes.",
+                        repo=repo,
+                        source=source,
+                    )
+                    self.assertEqual(result.package, name)
+                    self.assertTrue((repo / "tests" / f"test_{name}_profiles.py").is_file())
+                    source, ancestor = repo, name
+
     def test_creates_body_yaml_descendant_from_modern_source(self) -> None:
         with tempfile.TemporaryDirectory() as directory:
             workspace = Path(directory)
